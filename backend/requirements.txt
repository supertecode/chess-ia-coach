# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data validation & settings
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Chess engine
stockfish==3.28.0

# Async HTTP (used by adapters that call REST APIs directly)
httpx==0.28.1

# LLM provider SDKs (only the one matching LLM_PROVIDER is required at runtime)
groq==0.13.1
openai==1.59.6
anthropic==0.42.0
google-generativeai==0.8.3
